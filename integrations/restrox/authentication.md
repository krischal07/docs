---
title: Authentication
description: Standalone authentication summary for the outlet-owned RestroX docs.
sidebarTitle: Authentication
---

Use the native authentication page as the canonical reference.

## Current Summary

- merchant APIs use merchant authentication
- partner APIs use `x-partner-key`
- customer APIs use `x-partner-key` and `x-integration-key`
- webhooks use the integration token in `POST /webhook/restrox/{token}`
